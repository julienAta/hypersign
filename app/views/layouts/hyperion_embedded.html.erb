<!DOCTYPE html>
<html data-theme="docuseal" lang="<%= I18n.locale %>">
  <head>
    <%= render 'layouts/head_tags' %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <%= csrf_meta_tags %>
    <%= javascript_pack_tag 'application', defer: true %>
    <%= stylesheet_pack_tag 'application', media: 'all' %>
    <style>
      /* Hide any DocuSeal branding and navigation */
      .navbar, nav, header, footer, .sidebar {
        display: none !important;
      }
      
      body {
        background: #fefefe;
      }
      
      /* Full screen template builder */
      .hyperion-embedded-container {
        width: 100vw;
        height: 100vh;
        overflow: auto;
        padding: 0;
        margin: 0;
      }
      
      /* Hide back buttons and external links */
      a[href="/templates"], 
      a[href="/"],
      .breadcrumb {
        display: none !important;
      }
    </style>
  </head>
  <body>
    <turbo-frame id="modal"></turbo-frame>
    <turbo-frame id="drawer"></turbo-frame>
    
    <div class="hyperion-embedded-container">
      <%= yield %>
    </div>
    
    <%= render 'shared/body_scripts' %>
  </body>
</html>
